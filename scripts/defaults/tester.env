# Tester Service Environment Configuration
# Copy this file to /etc/tester/tester.env and customize as needed

# AWS SQS URLs for receiving submission and streaming responses
SUBM_REQ_QUEUE_URL=https://sqs.eu-central-1.amazonaws.com/975049886115/standard_submission_queue
RESPONSE_QUEUE_URL=https://sqs.eu-central-1.amazonaws.com/975049886115/standard_subm_eval_results

# NATS server URL (can be overridden with --url flag)
# NATS_URL=nats://localhost:4222

# Importantly, add Go to PATH
PATH=/usr/local/go/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin

# AWS Configuration (choose one method):

# Method 1: Use IAM role (recommended for EC2 instances)
# No additional configuration needed - the service will automatically use the instance's IAM role

# Method 2: Use AWS credentials file
# Place credentials in /home/tester/.aws/credentials

# Method 3: Use environment variables (less secure, not recommended for production)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_REGION=eu-central-1

# Optional: XDG Base Directory overrides (usually not needed)
# XDG_DATA_DIRS=/usr/local/share:/usr/share
# XDG_CONFIG_DIRS=/etc/xdg
